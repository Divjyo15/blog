<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Blogs</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 0; padding: 0; background-color: #f0f0f0; }
        nav { background-color: #333; padding: 10px; color: white; }
        .container { width: 90%; margin: 0 auto; }
        .nav-content { display: flex; justify-content: space-between; align-items: center; }
        .nav-links { display: flex; align-items: center; }
        .nav-links a, .dropdown-button { color: #ddd; text-decoration: none; margin-left: 20px; }
        .dropdown { position: relative; display: inline-block; }
        .dropdown-content { display: none; position: absolute; background-color: #f9f9f9; min-width: 160px; box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2); z-index: 1; }
        .dropdown-content a { color: black; padding: 12px 16px; text-decoration: none; display: block; }
        .dropdown-content a:hover { background-color: #f1f1f1; }
        .blog-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 20px; padding: 20px 0; }
        .blog-card { background-color: white; border-radius: 5px; padding: 15px; box-shadow: 0 2px 5px rgba(0,0,0,0.1); }
        .blog-card img { width: 100%; height: 150px; object-fit: cover; border-radius: 5px; }
        .blog-card h2 { font-size: 1.2em; margin: 10px 0; }
        .blog-card p { font-size: 0.9em; color: #666; }
        .delete-button { background-color: #ff4444; color: white; border: none; padding: 5px 10px; border-radius: 3px; cursor: pointer; }
    </style>
</head>
<body>
    <nav>
        <div class="container">
            <div class="nav-content">
                <div class="logo">Blog</div>
                <div class="nav-links">
                    <a href="/">Home</a>
                    <a href="/blog/add-new">Add Blog</a>
                    <div class="dropdown">
                        <button class="dropdown-button" id="userDropdown">
                            <%= user.fullName %> ▼
                        </button>
                        <div class="dropdown-content" id="userDropdownMenu">
                            <a href="/user/logout">Logout</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="blog-grid">
            <% blogs.forEach(blog => { %>
            <div class="blog-card">
                <% if (blog.coverImageURL) { %>
                <img src="<%= blog.coverImageURL %>" alt="Blog Cover Image">
                <% } %>
                <h2><%= blog.title %></h2>
                <p><%= blog.body.substring(0, 100) %>...</p>
                <form action="/blog/delete/<%= blog._id %>" method="POST">
                    <button type="submit" class="delete-button">Delete</button>
                </form>
            </div>
            <% }) %>
        </div>
    </div>

    <script>
        const userDropdown = document.getElementById('userDropdown');
        const userDropdownMenu = document.getElementById('userDropdownMenu');
        if (userDropdown) {
            userDropdown.addEventListener('click', () => {
                userDropdownMenu.style.display = userDropdownMenu.style.display === 'block' ? 'none' : 'block';
            });
        }
    </script>
</body>
</html>
